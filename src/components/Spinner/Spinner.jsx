// src/components/Spinner.jsx
import React from "react";
import "./Spinner.css"; // Nhập file CSS để áp dụng kiểu dáng

const Spinner = () => {
  return (
    <div className="spinner-container">
      <div className="spinner"></div>
    </div>
  );
};

export default Spinner;
